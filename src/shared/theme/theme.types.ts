export type Theme = Readonly<{
  accentSurface: string;
  background: string;
  border: string;
  error: string;
  muted: string;
  onPrimary: string;
  primary: string;
  surface: string;
  text: string;
}>;
